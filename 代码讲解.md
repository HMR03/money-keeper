# 记账本 PWA — 代码讲解（面向初学者）

这份文档逐部分讲解项目的代码结构和设计思路，适合想学习「纯前端 PWA 是怎么做出来的」的读者。

---

## 一、项目是什么

这是一个**纯前端**的记账应用，可以像原生 App 一样安装到手机主屏幕，断网也能用。

关键词：

| 概念 | 大白话解释 |
|------|-----------|
| PWA | Progressive Web App，网页穿上 App 的壳，能装到桌面、离线打开 |
| SPA | Single Page Application，只有一个 HTML 文件，靠 JS 切换页面 |
| IndexedDB | 浏览器内置的数据库，能存大量结构化数据 |
| localStorage | 浏览器内置的键值对存储，适合存少量配置 |
| OPFS | Origin Private File System，浏览器给每个网站分配的私密文件夹 |
| Service Worker | 网页的后台线程，能拦截网络请求，实现缓存和离线 |

---

## 二、文件结构

```
记账本PWA/
├── index.html          ← 整个 App 的唯一页面（HTML + CSS + JS 全在这）
├── manifest.json       ← PWA 配置文件（图标、名称、全屏模式等）
├── sw.js               ← Service Worker（离线缓存逻辑）
├── icons/
│   ├── icon-192.png    ← App 小图标
│   └── icon-512.png    ← App 大图标
├── 记账本完整使用指南.txt  ← 用户使用说明
└── README.md           ← 项目说明
```

**为什么只有一个 HTML 文件？**

因为项目足够小，把所有东西放一个文件里反而简单——不用装 Node.js、不用配 Webpack，浏览器直接打开就能跑。坏处是代码长了不好维护，适合小型项目。

---

## 三、index.html 逐段讲解

### 3.1 头部 `<head>`（第 1-11 行）

```html
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
```

- `viewport-fit=cover` → iPhone 刘海屏全屏显示
- `user-scalable=no` → 禁止用户双指缩放，更像原生 App

```html
<meta name="apple-mobile-web-app-capable" content="yes">
```

- 告诉 iOS Safari：这个网页可以全屏运行（隐藏地址栏）

```html
<meta name="theme-color" content="#FF6B35">
```

- 浏览器地址栏/状态栏的颜色，和 App 主题色保持一致

```html
<link rel="manifest" href="manifest.json">
```

- 指向 PWA 清单文件，浏览器靠这个知道怎么「安装」这个 App

### 3.2 CSS 变量（第 14-22 行）

```css
:root {
  --primary: #FF6B35;      /* 主色调：橙色 */
  --green: #2ECC71;        /* 收入颜色：绿色 */
  --red: #E74C3C;          /* 删除/危险操作：红色 */
  --bg: #F5F5F5;           /* 背景灰色 */
  --card: #FFFFFF;         /* 卡片白色 */
  --text: #1A1A2E;         /* 文字深色 */
  --text2: #8E8E93;        /* 次要文字灰色 */
  --text3: #C7C7CC;        /* 占位/禁用文字 */
  --border: #F0F0F0;       /* 分割线 */
  --radius: 14px;          /* 圆角 */
  --safe-b: env(safe-area-inset-bottom, 0px);  /* iPhone 底部安全区 */
}
```

**设计思路**：把所有颜色和尺寸定义成变量，改一处全局生效。这是 CSS 的最佳实践。

### 3.3 页面结构 —— 四页面 + 底部 Tab（第 143-273 行）

```
<div id="app">
  <div id="page-tx"    class="page active">  ← 流水页（默认显示）
  <div id="page-add"   class="page">         ← 记一笔
  <div id="page-report" class="page">        ← 报表
  <div id="page-settings" class="page">      ← 设置

  <nav class="tabbar">                       ← 底部导航栏
    📋 流水  |  📊 报表  |  ＋  |  ⚙️ 设置
```

**切换逻辑**：

```javascript
function showPage(id, tabBtn) {
  // 1. 隐藏所有页面
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));

  // 2. 显示目标页面
  document.getElementById('page-' + id).classList.add('active');

  // 3. 更新底部 tab 高亮
  // 4. 如果切到流水页就刷新列表，切到报表页就重绘图表
}
```

`display: none/block` 切换，不是真正的页面跳转。所有内容都在一个 HTML 里，切页面 = 切换 CSS 类名。

### 3.4 记一笔页面的数字键盘（第 188-202 行）

```
┌───┬───┬───┐
│ 1 │ 2 │ 3 │
├───┼───┼───┤
│ 4 │ 5 │ 6 │
├───┼───┼───┤
│ 7 │ 8 │ 9 │
├───┼───┼───┤
│ . │ 0 │ ⌫ │
├───┴───┴───┤
│  删除 │保存│
└───────────┘
```

`display: grid; grid-template-columns: repeat(3, 1fr)` → CSS Grid 三列布局。

`numInput(key)` 函数处理输入逻辑：

```javascript
function numInput(key) {
  if (key === 'del') {
    // 删除最后一个字符，剩一个时变回 '0'
    addAmount = addAmount.length > 1 ? addAmount.slice(0, -1) : '0';
  } else if (key === '.') {
    // 只能有一个小数点
    if (!addAmount.includes('.')) addAmount += '.';
  } else {
    // 避免前导零
    if (addAmount === '0') addAmount = key;
    else addAmount += key;
  }
  // 限制小数两位、总长 10 位
}
```

### 3.5 CSS 关键布局（第 28-41 行）

```css
#app {
  height: 100%;
  display: flex;
  flex-direction: column;   /* 从上到下排列 */
}

.page {
  flex: 1;                   /* 占满剩余空间 */
  overflow-y: auto;          /* 内容超出滚动 */
  display: none;             /* 默认隐藏 */
}
.page.active { display: block; }

.tabbar {
  position: fixed;           /* 固定在屏幕底部 */
  bottom: 0;
  z-index: 100;              /* 保证在最上层 */
}
```

**关键设计**：页面内容区 `flex: 1` + 底部 Tab `fixed`，保证 Tab 永远在屏幕底部不被内容顶走。

---

## 四、JavaScript 核心逻辑

### 4.1 数据存储 —— IndexedDB（第 319-371 行）

IndexedDB 是浏览器里的数据库，比 localStorage 能存更多数据，支持索引查询。

```
数据库名：MoneyKeeper（版本 1）
  └── 表：transactions
        ├── 主键：id（自动生成）
        └── 索引：yearMonth（按月查询）
```

一条记账记录的结构：

```javascript
{
  id: "abc123",           // 唯一 ID（时间戳 + 随机数）
  type: "expense",        // "expense" 支出 或 "income" 收入
  amount: 35.5,           // 金额
  category: "餐饮",        // 分类
  note: "午餐",           // 备注（可选）
  date: "2026-06-21",     // 日期
  yearMonth: "2026-06",   // 年月（用来按月查询）
  createdAt: 1718960000   // 创建时间戳
}
```

**核心数据库操作函数**：

| 函数 | 作用 |
|------|------|
| `openDB()` | 打开/创建数据库 |
| `dbAdd(tx)` | 新增或更新一条记录（put 是 upsert） |
| `dbDelete(id)` | 删除一条记录 |
| `dbGetByMonth(year, month)` | 按月查询（走索引，很快） |
| `dbGetAll()` | 读取全部记录 |
| `dbClear()` | 清空数据库 |

### 4.2 分类管理 —— localStorage（第 382-391 行）

分类用 localStorage 存，因为数据量小（十几个分类名），不需要数据库。

```javascript
// 读取分类（有自定义用自定义，没有用默认的）
function getCats(type) {
  const stored = localStorage.getItem(`mk_cats_${type}`);
  if (stored) return JSON.parse(stored);
  // 默认分类
  return type === 'expense' ? [...DEFAULT_EXPENSE_CATS] : [...DEFAULT_INCOME_CATS];
}
```

### 4.3 图片存储 —— OPFS（第 894-920 行）

OPFS 是浏览器给每个网站分配的私密文件夹，适合存文件。

```
图片流程：
拍照/选图 → compressImage() → Canvas 缩到 800px 宽 → JPEG 0.7 质量
           → saveImageToOPFS(txId, blob) → 存为 img_{txId}.jpg
```

**为什么压缩？**
- 手机照片通常 3-5MB，存原图浪费空间
- 压缩后约 60KB，记账凭证级别的清晰度完全够
- 压缩在 Canvas 里完成，不经过服务器

**图片生命周期**：

| 操作 | 发生的事 |
|------|---------|
| 新建 + 拍照 | 压缩 → `saveImageToOPFS()` |
| 编辑 + 换图 | 同名文件覆盖旧图 |
| 编辑 + 点「✕ 移除」 | `pendingImage = 'REMOVED'` → 保存时调 `deleteImageFromOPFS()` |
| 删除整条记录 | `dbDelete()` → `deleteImageFromOPFS()` |
| 不碰图片 | 什么都不发生，零影响 |

### 4.4 流水列表渲染（第 574-639 行）

```javascript
async function renderTxList() {
  // 1. 从数据库读取当月记录
  const txs = await dbGetByMonth(year, month);

  // 2. 按日期排序（新的在前）
  txs.sort((a, b) => b.date.localeCompare(a.date) || b.createdAt - a.createdAt);

  // 3. 计算汇总
  let totalExpense = 0, totalIncome = 0;
  txs.forEach(t => { ... });

  // 4. 按日期分组
  const groups = {};
  txs.forEach(t => {
    if (!groups[t.date]) groups[t.date] = [];
    groups[t.date].push(t);
  });

  // 5. 生成 HTML
  for (const [date, items] of Object.entries(groups)) {
    html += `<div class="day-header">${格式化日期}  ${当日合计}</div>`;
    items.forEach(t => {
      html += `<div class="tx-item" onclick="openAdd('${t.id}')"> ... </div>`;
    });
  }

  // 6. 异步检查哪些记录有图片
  txs.forEach(async (t) => {
    try {
      await getImageHandle(`img_${t.id}.jpg`);
      document.getElementById(`imgBadge_${t.id}`).style.display = '';
    } catch(e) { /* 没图片，忽略 */ }
  });
}
```

**关键设计**：点击流水条目 = 进入编辑模式（`onclick="openAdd('${t.id}')"`），传入记录 ID。

### 4.5 筛选功能 — 分类标签 + 备注搜索（第 648-717 行）

流水页新增两个筛选器，帮助在当月记录中快速定位：

```
┌───────────────────────────┐
│   🔍 搜索备注或分类...  ✕  │  ← 输入框，200ms 防抖
│  [全部] [餐饮] [水果] ... │  ← 标签栏，横向滚动
│  筛选结果：3 条 / 共 25 条 │  ← 提示行
└───────────────────────────┘
```

**分类标签栏**：自动从当月实际使用的分类中生成（没用过的不显示）。点一个选中，再点取消。`filterCat` 变量记录当前选中的分类。

**备注搜索**：`oninput` 触发，200ms 防抖。搜索范围覆盖分类名和备注字段，大小写不敏感。右侧有 ✕ 清除按钮。

**筛选逻辑**（AND 关系）：

```javascript
const txs = allTxs.filter(t => {
  if (filterCat && t.category !== filterCat) return false;
  if (filterQuery) {
    if (!t.category.toLowerCase().includes(q) &&
        !t.note.toLowerCase().includes(q)) return false;
  }
  return true;
});
```

**汇总跟着变**：筛选后的 `txs` 数组直接拿去算支出/收入/结余，所以汇总卡片显示的是筛选结果的小计。

**切换月份自动重置**：`changeMonth()` 里清空 `filterQuery` 和 `filterCat`，清空输入框。

### 4.6 批量操作 — 筛选 → 勾选 → 改 / 删

这是流水页的三步批量编辑流程：

```
第一步：筛选              第二步：勾选               第三步：操作
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│ 🔍 搜索...       │   │ ☐ 全选   [退出]  │   │ 已选 3 条        │
│ [餐饮] [水果]... │   │ ☑ 餐饮  -26.64  │   │ [改分类][改备注]  │
│                 │   │ ☐ 出行  -15.00  │   │ [删除]           │
│                 │   │ ☑ 水果  -8.50   │   └─────────────────┘
│ 筛选结果：2/25  │   │ ☐ 通讯  -100.00 │   底部浮出操作栏
│ [批量操作]      │   │                 │
└─────────────────┘   └─────────────────┘
   提示栏右侧按钮         每条记录左边出现      改分类：弹出分类网格
   点一下进入勾选模式      复选框 + 全选开关      改备注：查找替换 或 统一设置
                                              删除：二次确认后批量删除
```

**核心逻辑**：

- **入口**：筛选激活后提示栏出现「批量操作」按钮，点一下进入勾选模式
- **勾选**：`checkMode = true`，每条记录左边渲染复选框。全选/取消全选控制所有复选框。退出按钮回到正常模式
- **统计**：底部操作栏实时显示 `已选 N 条`
- **改分类**：弹出分类选择网格（复用现有的分类数据），选中后所有勾选记录的 `category` 统一修改，逐条 `dbAdd()` 更新
- **改备注**：两种模式
  - *查找替换*：`note.replace(旧文本, 新文本)` — 比如"美团外卖" → "外卖"
  - *统一设为*：所有勾选记录的备注改成同一个新值
- **删除**：确认弹窗 → 逐条 `dbDelete()` + `deleteImageFromOPFS()` → 退出勾选模式
- **收尾**：任何操作完成后自动退出勾选模式，刷新列表，触发 `backupToOPFS()`

### 4.7 记一笔与编辑共用（第 437-538 行）

新增和编辑用的是同一个页面 `#page-add`，靠 `editingTxId` 变量区分：

```javascript
function openAdd(txId) {
  editingTxId = txId || null;   // 有 txId = 编辑模式，没有 = 新建模式

  if (txId) {
    // 编辑模式：加载已有数据
    // 显示删除按钮
    document.getElementById('addTitle').textContent = '编辑';
  } else {
    // 新建模式
    document.getElementById('addTitle').textContent = '记一笔';
  }
}

function saveTx() {
  const tx = {
    id: editingTxId || 生成新ID(),   // 编辑用旧 ID，新建生成新 ID
    ...
  };
  dbAdd(tx);           // put 操作：ID 存在就更新，不存在就新增
  backupToOPFS();      // 每次保存后静默备份
  closePage();
}
```

### 4.8 报表页（第 629-712 行）

顶部是一张橙色汇总卡片，样式和流水页的月度卡片一致：

```
月模式 → 显示「本月支出 ¥1,634.50 / 收入 ¥5,000 / 结余」
年模式 → 显示「本年支出 ¥9,800 / 收入 ¥30,000 / 结余」
```

卡片跟着 `setReportPeriod()` 切换自动刷新数据。

下方是 Chart.js 折线图：

```
月度模式：显示近 12 个月的趋势
年度模式：显示近 5 年的趋势

支出（橙线）vs 收入（绿线）
```

支出分类排行用纯 CSS 进度条：

```html
<div class="rank-bar">
  <div class="rank-bar-fill" style="width: {百分比}%"></div>
</div>
```

### 4.9 数据导出/导入 + 支付宝 CSV（第 780-821 行 / 第 1009-1126 行）

```javascript
// 导出：把所有数据打包成 JSON 文件下载
async function exportData() {
  const data = {
    transactions: all,          // IndexedDB 记录
    expenseCategories: [...],   // 支出分类
    incomeCategories: [...]     // 收入分类
  };
  // 创建 Blob → 生成下载链接 → 自动点击下载
}

// 导入：读取 JSON 文件，覆盖当前数据
async function handleImport(e) {
  // 清空数据库 → 逐条写入 → 恢复分类
}
```

**支付宝 CSV 导入**（追加模式，不覆盖）：

```javascript
async function handleAlipayCSV(e) {
  // 1. 读取文件，找到「交易时间」表头行
  // 2. 逐行解析 CSV（支持引号内逗号）
  // 3. 过滤：不计收支、交易关闭、退款成功、金额为 0 → 全部跳过
  // 4. 去重：从 dbGetAll() 内存查已有的 alipayOrderNo，重复跳过
  // 5. 支付宝分类 → 记账本分类（餐饮美食→餐饮，14 条映射表）
  // 6. 弹窗预览统计，用户确认后批量写入
}
```

关键设计：不碰数据库结构（版本保持 v1），去重用内存查而不是建索引，避免触发数据库升级导致卡顿。

---

## 五、PWA 工作原理

### 5.1 manifest.json

```json
{
  "name": "记账本",
  "display": "standalone",    // 全屏，隐藏浏览器地址栏
  "theme_color": "#FF6B35",   // 状态栏颜色
  "orientation": "portrait",  // 锁定竖屏
  "icons": [...]
}
```

浏览器读到这个文件就知道：这是一个可以安装的 App，用这些图标，全屏运行。

### 5.2 Service Worker（sw.js）

```
用户第一次打开网页
  ↓
浏览器下载 sw.js
  ↓
SW 的 install 事件触发
  ↓
预缓存 index.html、manifest.json、图标、Chart.js CDN 到 CACHE
  ↓
下次用户打开（断网也能用）
  ↓
SW 的 fetch 事件拦截请求
  ↓
先从缓存找 → 找到就用缓存的（秒开）
            → 找不到才联网 → 联网成功存缓存
                            → 联网失败返回 index.html（离线兜底）
```

**为什么改代码后手机不更新？**

SW 使用 cache-first 策略：缓存有就直接用，不联网检查。

解决方法：每次改完代码，把 `sw.js` 第一行的 `CACHE_NAME` 版本号 +1：

```javascript
const CACHE_NAME = 'moneykeeper-v4'; // ← 改代码后 +1
```

浏览器检测到 sw.js 变了 → 安装新 SW → 新 SW 缓存最新文件 → 激活后删掉旧缓存。

### 5.3 OPFS 静默备份

```javascript
// 每次保存后偷偷多存一份
async function backupToOPFS() {
  const root = await navigator.storage.getDirectory();
  const file = await root.getFileHandle('moneykeeper_backup.json', { create: true });
  // 把全量数据写入
}

// 启动时检测：数据丢了？从备份恢复
(async function init() {
  const all = await dbGetAll();
  if (!all.length) {
    await restoreFromOPFS();  // 尝试恢复
  }
})();
```

**为什么需要这个？** IndexedDB 在极端情况下可能被浏览器清理（比如存储空间不足）。OPFS 文件更稳定，系统清理时不删。

---

## 六、数据流全景图

```
用户操作
  │
  ├─ 记一笔 ──→ saveTx() ──→ dbAdd() ──→ IndexedDB
  │                          └─→ saveImageToOPFS() ──→ OPFS（有图片时）
  │                          └─→ backupToOPFS() ──→ OPFS（全量 JSON）
  │
  ├─ 查看流水 ──→ renderTxList() ──→ dbGetByMonth() ← IndexedDB
  │                                └─→ 异步查 OPFS 图片标记
  │
  ├─ 编辑记录 ──→ openAdd(id) ──→ 加载数据 ← IndexedDB
  │            ──→ saveTx() ──→ dbAdd() 覆盖写入
  │            ──→ deleteTx() ──→ dbDelete() + deleteImageFromOPFS()
  │
  ├─ 查看报表 ──→ renderReport() ──→ dbGetAll() ← IndexedDB
  │                               └─→ Chart.js 绘图
  │
  ├─ 导出数据 ──→ dbGetAll() + getCats() ──→ JSON 文件下载
  │
  └─ 导入数据 ──→ 读取 JSON ──→ dbClear() + 逐条 dbAdd() + saveCats()
```

---

## 七、关键设计决策

| 决策 | 为什么 |
|------|--------|
| 单文件架构 | 项目小，一个 HTML 够用；初学者容易看懂 |
| IndexedDB 而不是 localStorage | 交易记录可能成千上万条，需要索引查询 |
| 图片存 OPFS 不存 IndexedDB | 二进制太大，污染数据库查询性能 |
| 数字键盘自建 | 手机上 `<input type="number">` 体验差，自建键盘可控 |
| Chart.js CDN | 纯报表页用，不安装 npm 包 |
| 纯前端无后端 | 隐私第一，数据都在用户自己手机上 |
| CSS 变量 | 统一配色，改一个变量全局生效 |

---

## 八、怎么自己改代码

- **换颜色**：改 `:root` 里的 `--primary` 变量，整个 App 的橙色全部变成你选的颜色
- **加分类**：改 `DEFAULT_EXPENSE_CATS` 或 `DEFAULT_INCOME_CATS` 数组
- **改图标**：换 `icons/` 目录下的 PNG 文件
- **调布局**：CSS 在 `<style>` 标签里，按页面分组有注释
- **加功能**：HTML 加结构 → CSS 加样式 → JS 加逻辑，按现有模式来

**改完代码别忘了**：`sw.js` 里 `CACHE_NAME` 版本号 +1，否则手机看不到更新。
